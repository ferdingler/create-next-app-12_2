version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install yarn
        - yarn install
    build:
      commands:
        - yarn run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
